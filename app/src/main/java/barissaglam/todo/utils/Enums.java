package barissaglam.todo.utils;

public class Enums {

    public enum Page{
        CREATE,
        DETAIL
    }

    public enum DatePickerAction {
        SELECT_TASK_DATE,
        SELECT_TASK_REMINDER_DATE
    }

    public enum ListSort{
        CUSTOM(),
        DATE,
        PRIORITY
    }
}
